{
  "name": "aibom-monorepo",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter ai-bom-viewer dev",
    "build": "pnpm --filter ai-bom-viewer build",
    "build:template": "pnpm --filter ai-bom-viewer build:template",
    "version": "changeset version",
    "release": "changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.5.0",
    "@changesets/cli": "^2.31.0"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "devEngines": {
    "packageManager": { "name": "pnpm", "version": ">=11.0.0", "onFail": "download" }
  }
}
